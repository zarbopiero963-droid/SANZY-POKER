# Struttura prevista

| Percorso                                  | Responsabilità                                                                                                  |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `client/src/pages/Home.tsx`               | Gate B2B: landing → NDA → sblocco → (lingua → variante) → canvas + timer                                        |
| `client/src/business/BusinessLanding.tsx` | Homepage B2B: slogan, pitch, CTA "Prova la demo", toggle IT/EN                                                  |
| `client/src/business/NdaDialog.tsx`       | Popup NDA click-wrap a 3 slide + schermata di sblocco con password                                              |
| `client/src/business/DemoTimer.tsx`       | Overlay timer demo 15' (non riparte al refresh; fasi colore calma→ambra→rosso)                                  |
| `client/src/business/DemoExpired.tsx`     | Schermata di blocco a demo scaduta                                                                              |
| `client/src/business/CookieBanner.tsx`    | Banner cookie GDPR (accetta/rifiuta, persistito; non blocca la landing)                                         |
| `client/src/business/LegalModal.tsx`      | Modal legale: Termini e Condizioni / Privacy / Cookie Policy                                                    |
| `client/src/business/useFocusTrap.ts`     | Hook focus trap + ripristino focus condiviso dai modali business                                                |
| `client/src/business/demoSession.ts`      | Logica pura: validazione, password, payload NDA, timer+fasi, consenso cookie, persistenza                       |
| `client/src/business/landingI18n.ts`      | i18n IT/EN della sezione business (`tb()`), separato dall'i18n del gioco                                        |
| `client/src/business/ndaService.ts`       | `submitNda(form, locale)` — `fetch` reale a `POST /api/nda/sign`; usa la risposta del server                    |
| `shared/ndaText.ts`                       | **Fonte unica** del testo legale NDA (IT/EN) + `NDA_VERSION` + `fillNdaText()`; client E server                 |
| `server/index.ts`                         | Express: static + `/healthz` + router NDA (`/api/nda`), `trust proxy: 1`, bind `0.0.0.0:PORT`                   |
| `server/nda/ndaText.ts`                   | Ri-esporta `shared/ndaText.ts` (import relativo, esbuild-friendly)                                              |
| `server/nda/signing.ts`                   | Schema zod, firma server-authoritative, PDF (pdf-lib) + `toWinAnsiSafe`                                         |
| `server/nda/store.ts`                     | Interfaccia `SignatureStore` (anti-replay + idempotenza) + impl. **in-memory** (`reserve`/`finalize`/`release`) |
| `server/nda/pgStore.ts`                   | Impl. **Postgres durevole** di `SignatureStore` (`pg`), attiva con `DATABASE_URL`; SQL `ON CONFLICT` atomico    |
| `server/nda/email.ts`                     | Invio NDA via Resend (owner + copia opt-in all'azienda) con degradazione senza `RESEND_API_KEY`                 |
| `server/nda/rateLimit.ts`                 | Rate limiter in-memory per IP (anti-abuso su endpoint pubblico costoso)                                         |
| `server/nda/router.ts`                    | `processNdaSign()` (puro) + `POST /sign` + rate limit + `extractClientIp()` (`req.ip`) + `maskEmail`            |
| `client/src/components/StartScreen.tsx`   | Selettore lingua e schermata logo + pulsanti Standard/Hi-Low                                                    |
| `client/src/components/SanzyLogo.tsx`     | Logo Sanzy Poker vettoriale (SVG) ricostruito dal marchio originale                                             |
| `client/src/components/GameCanvas.tsx`    | Ciclo di vita del motore grafico e canvas full-screen (prop `variant`)                                          |
| `client/src/game/i18n.ts`                 | Dizionari IT/EN/ES/FR, `t()`, `describeHand()`, `formatChips()`                                                 |
| `client/src/game/scene.ts`                | Creazione scena (accetta la variante), regia tavolo e smaltimento                                               |
| `client/src/game/rules.ts`                | Mazzo, combinazioni, confronto Standard/Hi-Low (label IT canonica)                                              |
| `client/src/game/state.ts`                | Stato tavolo, fasi, azioni e regola split 50/50 per piatto                                                      |
| `client/src/game/bots.ts`                 | Decisioni e personalità dei bot                                                                                 |
| `client/src/game/ui.ts`                   | Costruzione HUD, sedute, carte e controlli (testi via `t()`)                                                    |
| `client/src/game/viewSignature.ts`        | Firma pura dello stato visibile: render-gate anti-flicker dell'HUD                                              |
| `client/src/game/anim.ts`                 | Helper puri di animazione (pulsazione bordo attivo) usati da `tick()`                                           |

**Confine architetturale:** React ospita il flusso d'ingresso e il canvas; regole e gameplay restano classi e funzioni TypeScript indipendenti dal framework. L'i18n è un modulo puro senza logica di gioco: traduce solo le etichette mostrate; la `label` italiana in `rules.ts` resta la forma canonica usata dai test, mentre il rendering usa `describeHand()`.

**Sezione business (demo B2B con NDA, idea #12 · tracking #26).** Il modulo `client/src/business/**` aggiunge, _davanti_ al gioco, un imbuto commerciale: landing → popup NDA a 3 slide (click-wrap) → schermata di sblocco con password di sessione → gioco con timer demo di 15'. Vincoli: **non tocca il motore** (regole, stato, showdown, mazzo, fasi restano identici); la logica pura (validazione, password, payload NDA, timer) sta in `demoSession.ts` ed è coperta da test offline; l'i18n business è **IT/EN** e **separato** da quello del gioco (che resta a 4 lingue, così il test di completezza non si rompe — eccezione documentata). Il timer dipende solo da `startedAt` (salvato in `localStorage` alla firma) quindi **non riparte al refresh**; l'overlay ha tre **fasi colore** (calma → ambra negli ultimi 5' → rosso lampeggiante nell'ultimo minuto) calcolate da `timerPhase()`. La landing include il **pacchetto legale GDPR**: footer con Termini e Condizioni / Privacy Policy / Cookie Policy (aperti in `LegalModal`) e un **banner cookie** (`CookieBanner`) la cui scelta è persistita (`saveCookieConsent`/`loadCookieConsent`). `ndaService.submitNda(form, locale)` chiama l'endpoint reale.

**Backend NDA (PR2, tracking #26).** `server/nda/**` implementa `POST /api/nda/sign`, **server-authoritative**: il server valida il modulo (zod), genera `signatureId`/`password`, fissa `startedAt` (istante server) e rileva l'**IP con `req.ip`** (`trust proxy: 1` → non spoofabile via `X-Forwarded-For`); produce il PDF dell'NDA firmato (`pdf-lib`, testo sanitizzato WinAnsi contro nomi non-latini), logga IP/timestamp (email **mascherata**) e invia l'email col PDF a **pier.zar69@gmail.com** via **Resend**. Una **copia** del PDF al firmatario (spec #26, «copia all'azienda») è opt-in con `NDA_COPY_TO_COMPANY=1` — invio **separato** (il prospect riceve un'email dedicata, non è in copia con l'owner nel `To`; `replyTo` = owner instrada VOLUTAMENTE le risposte al contatto commerciale, quindi l'email owner è visibile negli header — desiderato; subject e testo localizzati IT/EN) e **fire-and-forget** (parte in background, fuori dal percorso critico, così non aggiunge latenza al budget client di 15s; un fallimento è solo loggato senza PII). Recapitabile solo con un dominio verificato (`RESEND_FROM`), quindi l'owner accende il flag quando ce l'ha. La risposta porta `companyCopyRequested` (invio avviato, non conferma di consegna) e lo sblocco mostra «Stiamo inviando una copia … alla tua email aziendale (controlla anche lo spam)». Il client USA la risposta (non impone valori propri) e la valida (`Number.isFinite`, stringhe non vuote). **Testo NDA = fonte unica** (`shared/ndaText.ts`) usata sia per la visualizzazione client sia per il PDF server: il testo accettato è identico a quello registrato (requisito click-wrap); `NDA_VERSION` è una sola costante condivisa. Invarianti: **anti-replay** = una demo per email, con prenotazione **atomica** (`reserve`, vedi PR3 sotto); riuso email (chiave d'idempotenza diversa) → 409; versione non combaciante → 422; body invalido → 400; caratteri di controllo nei campi liberi → 400 (anti-injection); troppe richieste per IP → **429** (rate limiter in-memory). Il `release` (rollback) scatta solo su **errore Resend transitorio** (→ 503 ritentabile) o errore interno; con `no-api-key` la prenotazione **resta** (si degrada l'email, non l'anti-replay). Il countdown demo è ancorato al clock del **client** (skew-proof in entrambe le direzioni); il tempo autorevole della firma è nel record/PDF server. Il cuore è `processNdaSign()`, **puro da Express** (deps iniettabili), testato offline (concorrenza inclusa). **Store durevole + idempotenza (PR3, #30 punto 1):** `SignatureStore` ha due impl. dietro la stessa interfaccia — **in-memory** (fallback quando `DATABASE_URL` è **assente** — dev/CI/effimero) e **Postgres** (`pgStore.ts`, attiva quando `DATABASE_URL` è **presente**; se l'init fallisce NON degrada in silenzio — l'errore si propaga e lo startup fallisce con `process.exit(1)`, così una config rotta è evidente invece di perdere la durabilità di nascosto). `reserve(entry)` è un check-and-set atomico che classifica l'esito: `reserved` / `replay` (stessa `idempotencyKey` → recupero della sessione) / `duplicate_email` (email già firmata con altra chiave → 409). Il client genera e **persiste** una `idempotencyKey` per email PRIMA dell'invio (`idempotencyKeyFor`), così un **retry dopo una risposta persa** recupera la stessa sessione (signatureId+password) invece di restare escluso con 409. In Postgres l'atomicità è dei vincoli PRIMARY KEY / UNIQUE (`INSERT ... ON CONFLICT DO NOTHING`); testata offline con `pg-mem`. Limiti dichiarati residui: senza `DATABASE_URL` lo store resta **in-memory** (Railway effimero → si azzera al redeploy); l'email **degrada senza `RESEND_API_KEY`** (Secret **una-tantum dell'owner**, mai nel repo). Nessun file del motore è toccato. Test full-stack locale: `pnpm build && pnpm start`.
