# Memory

- Il regolamento ufficiale usa 32 carte, 6 carte personali, Piatto 1 da 6 carte e Piatto 2 da 2 carte.
- La versione richiesta conserva la fase extra di scarto già presente nel prototipo.
- Divisione del piatto (regola definitiva confermata dall’autore): il totale vale 100, Piatto 1 = 50 e Piatto 2 = 50. Ogni metà è divisa in parti uguali tra i vincitori (o pareggianti) di quel piatto, in modo indipendente; chi vince entrambi da solo prende il 100%. Nell’heads-up a 2 giocatori questo produce comunque l’effettivo 750/250. (La vecchia formula 75/25 per 3-4 giocatori è stata scartata perché contraddiceva "ogni piatto vale 50".)
- La lobby deve mostrare bot effettivamente attivi e tavoli con stato di gioco percepibile.
- **Lingua business: auto-rilevamento + selettore esplicito (fix segnalato dall'owner).** L'owner ha notato che il vecchio **toggle** lingua della landing B2B sembrava «invertito»: era un pulsante singolo che mostrava la lingua di **DESTINAZIONE** (in italiano diceva «EN», in inglese «IT») — corretto come logica (cliccarlo cambiava lingua) ma controintuitivo, letto come indicatore della lingua CORRENTE. Sostituito con un **selettore a due pulsanti espliciti IT | EN** (`role="group"`, `aria-pressed` sull'attivo, evidenziazione): cliccare «EN» → inglese, «IT» → italiano, senza ambiguità. In più, **auto-riconoscimento della lingua dal browser** alla prima visita: `detectBizLocale(navigator.languages ?? navigator.language)` — funzione **pura e deterministica** (testabile senza DOM) che scorre le preferenze in ordine e sceglie la prima nota (italiano → `it`, qualunque altra lingua → `en`, target B2B estero; nessuna info → default `it`). Una scelta esplicita salvata in `localStorage` (`sanzy.business.locale`) ha **sempre la precedenza** sull'auto-rilevamento. Il selettore di lingua del **gioco** (StartScreen, 4 flag IT/EN/ES/FR) era ed è corretto (flag→locale e onClick giusti): il problema era solo la landing business. Nessun file del motore toccato; 278 test verdi (6 nuovi su `detectBizLocale`: ordine di preferenza, regione/maiuscole, lingua estera→en, default, validità del risultato). Verifica UI con smoke Playwright (browser IT→it, EN→en, FR→en; click EN→testo inglese).
- **Offuscamento del bundle + rimozione tooling Manus (richiesta owner «nessuno deve vedere la meccanica»).** Decisioni: (1) **Offuscamento SOLO dell'output di produzione**, non del sorgente nel repo (privato): offuscare i `.ts` committati romperebbe la suite di test hard obbligatoria, il typecheck e la manutenibilità (regole CLAUDE.md). Il sorgente resta leggibile; è illeggibile solo ciò che il browser scarica. (2) `vite.config.ts` usa `vite-plugin-javascript-obfuscator` (`enforce: "post"`, `apply: "build"`, `exclude: [/node_modules/]`) → offusca **solo** il nostro codice (mazzo/regole/confronti/bot/scena/UI), lasciando intatti Babylon.js e React (offuscarli gonfierebbe il bundle e rallenterebbe il render loop). Profilo MODERATO: mangling identificatori (hex), string array base64 + rotate/shuffle + split, `transformObjectKeys`, `numbersToExpressions`, control-flow flattening a 0.5, dead-code injection a 0.2. `selfDefending`/`debugProtection` OFF (possono rompere l'app o degradare le performance del gioco; accendibili solo con smoke-test reale). `build.sourcemap: false` è essenziale (una sourcemap pubblicata annullerebbe l'offuscamento). (3) **LIMITE ONESTO (comunicato all'owner):** il gioco è client-side → il codice è comunque scaricato da ogni visitatore; l'offuscamento alza molto il muro ma NON rende la meccanica invisibile al 100%. L'unico modo per nasconderla DAVVERO è spostarla lato server (tracciato in #30 punto 2, «sessione server-authoritative»). Scelta owner: «entrambi» — offuscamento ora + server-side come lavoro successivo. (4) **Rimosso tutto il tooling Manus** dell'ambiente di sviluppo: `client/public/__manus__/debug-collector.js` (script che intercettava console/network/interazioni utente e le POST-ava a `/__manus__/logs`), `client/src/components/ManusDialog.tsx` (codice morto, mai importato), i plugin Vite `vitePluginManusDebugCollector`/`vitePluginManusRuntime`/`vitePluginStorageProxy`, gli `allowedHosts` `.manus*`, e **`@builder.io/vite-plugin-jsx-loc`** (`jsxLocPlugin`, che iniettava percorsi-file + numeri di riga del sorgente dentro il DOM dell'app pubblicata — controproducente per l'offuscamento). Dipendenze rimosse: `@builder.io/vite-plugin-jsx-loc`, `vite-plugin-manus-runtime`. **Nessun file del motore toccato** (gettoni/50-50/gerarchia/mazzo/fasi/varianti invariati; 272 test verdi). Verifica offuscamento = ispezione dell'output buildato (grep degli identificatori del motore in chiaro → assenti), non un test vitest (il `dist/` non esiste in fase test).
- Flusso d'ingresso (scelta dell'autore): all'apertura dell'URL si sceglie PRIMA la lingua (IT/EN/ES/FR, default IT), poi compare il logo Sanzy Poker con due pulsanti di variante (Standard / Hi-Low); la variante scelta avvia la partita. Non c'è più la lobby a lista (mai raggiunta nel flusso attuale).
- Internazionalizzazione: `client/src/game/i18n.ts` traduce SOLO le etichette; non tocca regole né matematica. La lingua di default è l'italiano, così le stringhe attese dal motore (es. `Vittoria per fold`) restano invariate e i test restano verdi. `describeHand()` genera i nomi delle mani dai campi strutturati di `HandEvaluation` senza modificare `rules.ts`.
- Logo: `client/src/components/SanzyLogo.tsx` è la ricostruzione vettoriale del tavolo-marchio originale fornito dall'autore (feltro verde ovale, "SANZY" bianco, "POKER" rosso, board dei due piatti 3+2+1 e i due del Piatto 2, quattro semi agli angoli). La grafica 3D del tavolo di gioco resta da migliorare: vedi `HANDOFF-DESIGN.md`.
- Demo B2B con NDA (idea #12 · tracking #26): davanti al gioco c'è un imbuto commerciale (landing → popup NDA click-wrap a 3 slide → schermata di sblocco con password di sessione → gioco con timer demo di 15'). Decisioni con l'autore: (1) approccio a PR separate, frontend prima (PR1) e backend dopo (PR2); (2) testi landing/NDA in **IT/EN** soltanto (target B2B esteri) — vivono in `client/src/business/landingI18n.ts`, un i18n **separato** da quello del gioco, che resta a 4 lingue (così il test di completezza del gioco non si rompe); (3) provider email **Resend** per il PR2 (chiave come Secret `RESEND_API_KEY`, mai nel repo). Il timer demo dipende solo da `startedAt` salvato in `localStorage` alla firma: al refresh PROSEGUE e non riparte; a 0 blocca la demo. L'NDA firmato dovrà arrivare via email a pier.zar69@gmail.com (PR2). Il modulo `business/**` NON tocca il motore di gioco. Nota: il valore legale del click-wrap dipende dal testo e da revisione legale — il codice fornisce solo il meccanismo (dati, checkbox, PDF, log IP/ora, ID firma, email).
  - **Limiti noti del PR1 (frontend), risolti nel PR2 (backend):** (1) la **password di sessione** mostrata nella schermata di sblocco è per ora **solo informativa** — non c'è un form che la richiede, perché la verifica autorevole sarà lato server nel PR2; (2) il **gate dei 15'** è client-side e quindi aggirabile (svuotando `localStorage`, o tornando alla home e **rifirmando** l'NDA si ottiene un nuovo `startedAt`): l'ancoraggio anti-abuso — timer/password validati dal server e firma già usata rifiutata — arriva nel PR2. **Testo veritiero nel PR1 (decisione owner):** poiché nel PR1 non viene registrato nulla (email/PDF/log IP = PR2), il testo NDA e la schermata di sblocco NON affermano che IP/firma sono già stati registrati — usano il futuro («all'invio verrà registrato…», «Accesso demo sbloccato»). Il testo pieno («registrato con successo / IP registrato») verrà ripristinato nel PR2 quando la registrazione è reale. Il dialog NDA ha focus trap + ripristino del focus + sfondo `inert` (accessibilità del consenso). La password è generata con `crypto.getRandomValues` (rejection sampling, nessun modulo bias) e la sessione è persistita SUBITO alla firma (non al click su "Avvia"), usando `signatureId`/`password` dalla risposta di `submitNda` (nel PR2 autorevoli dal server). Alla scadenza la sessione viene rimossa da `localStorage` (`clearDemoSession`), sia via `onExpire` sia al mount. **Privacy:** in `localStorage` si salva solo il minimo (`signatureId`, `password`, `startedAt`) — la PII del payload (nome/email/azienda/ruolo) NON è persistita: resta in memoria e sarà inviata al server solo alla firma (PR2). **(PR3, #30):** si aggiunge una **mappa d'idempotenza pseudonima** — indicizzata per un **identificatore a 64 bit derivato dall'email** (due FNV-1a concatenati, non l'email in chiaro; ~64 bit → collisioni trascurabili per l'uso come **indice locale** di un singolo browser, non è un hash crittografico né anonimizzazione forte) con valore `{chiave casuale nanoid, ts}` e **TTL 45 giorni** (> `ROW_RETENTION_MS` server 30gg con margine, vedi sotto: DEVE essere STRETTAMENTE > o si reintroduce il lockout 409) **+ pruning** — per recuperare la firma dopo una risposta persa senza persistere PII in chiaro; la Privacy Policy IT/EN cita questa chiave tecnica. **Anti-manomissione del timer:** il guard `isPersistedSession` scarta uno `startedAt` **futuro** (`startedAt <= now`, `now` iniettabile per test) — niente più tolleranza di clock skew, che avrebbe permesso una demo fino a 20': un timestamp manomesso in avanti viene rifiutato e la sessione ricaricata è `null`. **Auditabilità del click-wrap:** `NdaPayload` porta anche `ndaVersion` (costante `NDA_VERSION`, oggi `pr1-draft-1`; il PR2 alza la versione col testo legale pieno) e `ndaLocale` (lingua in cui l'NDA è stato letto e accettato). **Accessibilità del timer:** l'annuncio screen-reader usa una region `role="status" aria-live="polite"` **stabile** (sempre presente, vuota fuori dall'ultimo minuto) invece di un toggle di `aria-live` sullo stesso nodo (inaffidabile). **Nota PR2:** la `password` è oggi in chiaro in `localStorage` (accettabile perché chiave client-side, non credenziale reale); quando il PR2 la renderà server-authoritative NON andrà persistita in chiaro (solo `signatureId` + riconvalida server) — TODO segnato in `saveDemoSession`. **Pacchetto legale GDPR (PR1, richiesto dall'owner nei commenti #26):** la landing ha un **footer** con Termini e Condizioni / Privacy Policy / Cookie Policy aperti in `LegalModal` (focus trap + Esc + `aria-labelledby`), e un **banner cookie** (`CookieBanner`) in fondo alla pagina: la scelta accetta/rifiuta è persistita in `localStorage` (`sanzy.cookies.accepted` via `saveCookieConsent`/`loadCookieConsent`; `null` = non ancora deciso → banner visibile) e non blocca la lettura. La Privacy Policy dichiara onestamente che nel PR1 (frontend) i dati NON lasciano il browser: email/PDF/log IP sono la fase backend (PR2). **Fasi colore del timer (idea #12):** `timerPhase()` (pura, testata) mappa il residuo in `calm` (>5') → `warn` ambra (≤5') → `urgent` rosso lampeggiante (≤1'); l'animazione di pulsazione rispetta `prefers-reduced-motion`. Il testo NDA resta **ammorbidito** nel PR1 (decisione owner confermata): il testo legale pieno «blindato» IT/EN dei commenti #26, con clausola click-wrap e segnaposto `{IP}/{TIMESTAMP}/{SIGNATURE_ID}`, entra nel PR2 quando la registrazione avviene davvero (alzando `NDA_VERSION`).
  - **PR2 (backend NDA) — implementato in `server/nda/**`, endpoint `POST /api/nda/sign`.** Decisioni: (1) **server-authoritative** — il server valida (zod), genera `signatureId`/`password`, fissa `startedAt`(istante server) e rileva l'IP; il client invia solo modulo +`ndaLocale`+`ndaVersion` e USA la risposta (`ndaService.submitNda(form, locale)`fa`fetch`reale). (2) **Anti-replay = una demo per email aziendale**:`SignatureStore` in-memory, riuso → **409** (`already_signed`, messaggio i18n `nda.error.alreadySigned`). **LIMITE dichiarato:** lo store è **in-memory** (Railway è effimero: si azzera al redeploy) — deterrente, non sicurezza forte; la durabilità (Postgres) è un'evoluzione isolata dietro l'interfaccia `SignatureStore`. (3) **PDF** con `pdf-lib`(deterministico: date azzerate, IP/timestamp passati come argomenti). (4) **Email** via **Resend** con **degradazione**: senza`RESEND_API_KEY`l'endpoint risponde comunque`ok`con`serverAcknowledged:false`(import dinamico di`resend`solo se c'è la chiave). **Azione owner una-tantum:** impostare il Secret`RESEND_API_KEY` (mai nel repo); il "from" richiede un dominio verificato su Resend (in test mode Resend consegna solo all'email dell'account, cioè quella dell'owner → funziona). (5) **`NDA_VERSION`alzata a`1.0-clickwrap`** e allineata client (`demoSession.ts`) ↔ server (`ndaText.ts`): versione non combaciante → **422**. Testo NDA legale pieno ripristinato lato client (`nda.body`, forma "verranno registrati" perché mostrato PRIMA dell'invio) e nel PDF server (segnaposto riempiti con IP/timestamp/ID reali). (6) `.sanzy-nda\_\_ndabox`ora`white-space: pre-line`per i paragrafi. Cuore testabile:`processNdaSign()`è **puro da Express** (deps iniettabili) → 22 test hard offline (validazione, formato firma/password, header`%PDF`, anti-replay 409, versione 422, degradazione email, il client non può imporre signatureId/password). **Nessun file del motore toccato** (gettoni/50-50/gerarchia/mazzo/fasi invariati). Test full-stack locale: `pnpm build && pnpm start`(Vite dev serve solo il frontend; in produzione Railway l'Express serve static +`/api` same-origin).
    - **Hardening PR2 (review Fable+GPT su #28):** (a) **Testo NDA = fonte unica** `shared/ndaText.ts` importata da client (visualizzazione, con nome/azienda/email riempiti e IP/timestamp/ID mostrati come «registrato all'invio») e server (PDF, segnaposto riempiti coi valori reali): il testo accettato è IDENTICO a quello registrato — prima client e server avevano testi diversi sotto la stessa versione (rischio click-wrap). `NDA_VERSION` è ora una sola costante condivisa (test client↔shared↔server). Rimosse `nda.body`; aggiunte `nda.recordedOnSubmit` e `nda.error.outdated`. (b) **IP non spoofabile:** `req.ip` con `trust proxy: 1` (Railway 1 hop) invece del primo elemento di `X-Forwarded-For` (controllabile dal client); fallback all'ultimo elemento XFF. (c) **Anti-replay atomico + rollback:** `tryRecord` (check-and-set atomico, no race → niente 500 su concorrenza) e `release` che libera la prenotazione se l'email NON è inviata (Resend degradato/errore) — una firma non viene persa né blocca i retry con 409 quando non c'è artefatto durevole. `SignatureStore` è async (seam Postgres). (d) **PDF robusto:** `toWinAnsiSafe` sostituisce i caratteri non-latini (CJK/cirillico/emoji) che farebbero lanciare `drawText` → 500. (e) **GDPR log:** email mascherata (`j***@dominio`). (f) **Client:** valida la risposta (`Number.isFinite(startedAt)`, stringhe non vuote) e messaggio dedicato per il 422 (`nda.error.outdated`). 217 test verdi (concorrenza, allineamento versione, sanitizzazione, rollback inclusi). **Fix round 2 (review su `577aab1`):** (i) **BUG critico PDF**: `renderNdaPdf` faceva `toWinAnsiSafe` PRIMA dello `split("\n")` → i newline (< 0x20) venivano cancellati e il PDF collassava in un unico paragrafo di `?`; ora si SPLIT-a prima e si sanitizza per riga (`sanitizedNdaParagraphs`, testato: >15 righe, nessun `?`). (ii) `toWinAnsiSafe` corretta a whitelist reale (ASCII 0x20–0x7E + Latin-1 0xA0–0xFF): esclusi anche i controlli C1 0x80–0x9F (non mappati da WinAnsi → altro potenziale 500). (iii) **Clock skew**: il client clampa `startedAt = min(server, Date.now())` così un orologio client indietro non fa scartare al refresh la sessione appena firmata. (iv) **Errore email transitorio** (`reason:"error"`, ≠ chiave assente) → **503** ritentabile (niente credenziali su firma non registrata); `no-api-key` resta modalità degradata dichiarata (200). (v) `release(email, signatureId)` guardato per signatureId; `400` non espone più i dettagli zod. 219 test verdi. **Fix round 3 (review su `8f6a894`):** (A) **Anti-injection:** lo schema zod vieta i caratteri di controllo (`\n`/`< 0x20`/`0x7F`) in `fullName`/`companyName`/`jobTitle` — senza, un `\n` iniettato forgerebbe righe di audit nell'email, paragrafi arbitrari nel PDF legale (lo split è su `\n`) o header nel subject. (B) **Rate limiting:** `server/nda/rateLimit.ts` (in-memory per IP, default 8/10', iniettabile) → `429` sull'endpoint pubblico costoso (PDF+email), anti-abuso quota Resend/inbox/CPU. (C) **Clock skew (entrambe le direzioni):** il countdown demo è ancorato al clock del CLIENT (`startedAt = Date.now()` alla firma), non al timestamp server — così né un client indietro (sessione scartata al refresh) né uno avanti (demo subito scaduta) rompono il timer; il tempo autorevole resta nel record/PDF server. (D) **`no-api-key` degrada l'EMAIL, non l'anti-replay:** in modalità senza chiave la prenotazione NON viene rilasciata (una demo per email tiene comunque, in-memory); il `release`+`503` resta solo per l'errore Resend TRANSITORIO (`reason:"error"`, con `email.detail` loggato senza PII). (E) client: `429 rate_limited` → messaggio dedicato `nda.error.rateLimited`. **Password in `localStorage`:** resta persistita in chiaro anche nel PR2 (è la password di sessione mostrata all'utente, gate client-side); la rimozione totale del plaintext richiede la **riconvalida server della sessione** (endpoint di validazione + store durevole) = **PR3**. 221 test verdi. **Fix round 4 (review su `a1b0e73`):** (I) **Rate limiter senza leak:** sweep periodico delle entry scadute (ogni 500 `check`) — prima gli IP visti una volta restavano nella Map per sempre. (II) **`payload.acceptedAt` = timestamp del server** (non il clock locale del countdown): stesso `signatureId` = stesso istante d'accettazione ovunque. (III) **Fail-closed opt-in:** env `NDA_REQUIRE_EMAIL=1` → senza email consegnata l'endpoint risponde **503** (rollback), invece di concedere la demo in degradato; default resta degradato (la demo funziona senza chiave). 223 test verdi. **Fix round 5 (review su `cf05378`):** (α) **Header-injection difesa in profondità nell'email:** `stripControl` rimuove i controlli (`\x00-\x1f\x7f`) da subject, filename e da ogni campo del corpo — `sendNdaEmail`/`buildEmailText`/`buildEmailSubject` sono esportate e potrebbero ricevere input non validati dallo schema zod; il subject resta a riga singola anche con `\r\n` iniettato. (β) **Rotte `/api/*` sconosciute → 404 JSON** prima del catch-all SPA: un client API che sbaglia path riceve `{ok:false,error:"not_found"}`, non l'`index.html` con 200. (γ) **Diagnostica `no-api-key + NDA_REQUIRE_EMAIL`:** il 503 fail-closed senza chiave logga una causa esplicita (config strict senza Secret) invece di un 503 muto. 226 test verdi. **Fix round 6 (review Fable+GPT su `cf05378`/`ae24992`):** (a) **`replyTo` sanitizzato:** `stripControl` mancava su `replyTo: input.businessEmail` in `sendNdaEmail` — unica via d'iniezione header rimasta scoperta nel file che dichiara la difesa in profondità; ora coperta. (b) **Contratto API su body invalido:** error handler del parser JSON nel router (`SyntaxError` → `400 {ok:false,error:"invalid_request"}`; `entity.too.large` → `413 payload_too_large`) invece dell'HTML del default handler Express — coerente col 404 JSON. Test d'integrazione HTTP reale (server effimero, `fetch`). (c) **Rate limit PRIMA del parse:** il `limiter.check` è ora un middleware che precede `express.json`, così un IP oltre soglia non paga il parse (fino a 16kb) dell'endpoint pubblico; test HTTP che prova l'ordine (429 anche con JSON malformato). (d) **`startedAt` client rifiutato fuori range Date:** `Number.isSafeInteger` + `≤ 8.64e15` — un valore enorme superava `Number.isFinite/>0` ma faceva lanciare `new Date(startedAt).toISOString()` (RangeError → schermata rotta); test su `submitNda` con `fetch` mockato. (e) **`maskEmail` local-part di 1 carattere:** `a@x.com` → `***@x.com` (prima `a***@…` rivelava l'unico carattere) — log dichiarato GDPR. 234 test verdi. **Fix round 7 (review Fable+GPT su `a59a90f`):** (I) **Privacy Policy corretta (compliance):** il testo legale del PR1 dichiarava «in questa fase i dati NON vengono inviati ad alcun server: restano nel tuo browser» — con il backend PR2 attivo diventa materialmente FALSO (i dati vengono inviati al server, l'IP registrato, un PDF firmato inviato via email all'owner tramite Resend). Riscritta la sezione `legal.privacy.body` IT/EN: aggiunta la voce «Destinatari» (server + Resend), rimossa la falsa nota, «Dati raccolti» al presente attivo. Test di regressione in `business.landingI18n.test.ts` (la privacy NON deve più affermare il claim e DEVE citare server+Resend). (II) **Verificati come non-issue (thread con evidenza, nessuna patch):** `saveDemoSession` è già protetta da try/catch interno (swallow di `localStorage`), quindi `setSession(finalized)` gira sempre → il presunto lockout post-firma di GPT è un falso positivo; `pdf-lib`/`resend` NON finiscono nel bundle client (grep del build: 0 occorrenze) → l'import dinamico di `resend` e la separazione client/server tengono. 235 test verdi. Boundary confermato PR3 (thread PR con evidenza): i18n a 4 lingue (eccezione owner per il funnel B2B; il testo NDA legale passa da `shared/ndaText.ts` a fonte unica, non da `t()`, per garantire testo mostrato == registrato), idempotenza su risposta persa + store durevole, sessione server-authoritative, font Unicode PDF, chiave rate-limit composita per CGNAT. **Fix round 8 (review Fable+GPT+CodeRabbit su `f89b0d7`):** (1) **BUG integrità testo legale — placeholder injection in `fillNdaText`:** la vecchia sostituzione sequenziale (`.replace()` per chiave, che ri-scansiona il testo già sostituito) permetteva a un valore utente contenente a sua volta un segnaposto (es. azienda `"Foo {SIGNATURE_ID}"`) di ri-espandersi e alterare il testo NDA canonico usato SIA per l'anteprima SIA per il PDF firmato (rottura «accettato == registrato»). Riscritta a **passata unica** su regex statico `\{(NOME|AZIENDA|EMAIL|IP|TIMESTAMP|SIGNATURE_ID)\}` (i valori sostituiti restano letterali; elimina anche il warning ReDoS da RegExp costruito da variabile). Test di regressione dedicato. (2) **Hang su invio email → prenotazione bloccata:** `resend.emails.send` non espone `AbortSignal` nella superficie pubblica dell'SDK; aggiunto `withTimeout` (`Promise.race`, default 15s) che rigetta e fa rilasciare la prenotazione anti-replay (503) invece di tenerla appesa a tempo indefinito. Test: risolve se veloce, rigetta se oltre timeout. (3) **Hang sulla `fetch` client → dialog bloccato:** `submitNda` senza timeout lasciava `submitting=true` per sempre (Esc/chiudi disabilitati) su connessione appesa; aggiunto `AbortController` + `setTimeout(15s)` con `clearTimeout` in `finally`, l'abort cade sul percorso `FAIL("network")`. Test con fake timers che verifica abort→network. (4) **Difesa in profondità email allineata al PDF:** `stripControl` esteso ai controlli C1 (`\x7f-\x9f`) e ai separatori di riga Unicode `U+2028/U+2029` (coerente con la whitelist WinAnsi del PDF che esclude i C1); nuovo `safeAttachmentId` (`[A-Za-z0-9_-]`) per il filename dell'allegato (`Content-Disposition` robusto anche con `signatureId` ostile). 241 test verdi. **Non-gate/by-design ribaditi in-thread (nessuna patch):** i18n a 4 lingue (eccezione owner modulo business, testo NDA da `shared/ndaText.ts`), fail-open senza `RESEND_API_KEY` (dev/CI, opt-in `NDA_REQUIRE_EMAIL=1` in prod), idempotenza/store durevole/sessione server-authoritative (PR3), `trust proxy: 1` Railway, rate-limit/anti-replay in-memory. Policy di convergenza (inseguire bug reali, non ciclare sul by-design) annotata in `CLAUDE.md`/`AGENTS.md` su richiesta owner. **Follow-up PR3 (in `todo.md`):** sessione/token firmato server-authoritative + `expiresAt` verificato, store durevole (Postgres) con idempotenza sulla risposta persa, font Unicode nel PDF (fontkit) per nomi non-latini. **Copia dell'NDA all'AZIENDA (spec #26, PR #29 dopo #28):** la guida #26 chiedeva l'NDA firmato «a pier.zar69@gmail.com + copia all'azienda» — in PR2 era stato implementato solo l'owner (buco tracciato su #26). Ora `sendNdaEmail` invia anche una COPIA del PDF al firmatario, con **invio SEPARATO** (`to: [businessEmail]`, non `to: [owner, prospect]`): il prospect riceve un'email dedicata e non è in copia con l'owner nel `To`; `replyTo` = owner instrada VOLUTAMENTE le risposte al contatto commerciale (l'email owner è quindi visibile negli header della copia — desiderato, non un leak); subject e testo **localizzati IT/EN** per `ndaLocale` (un firmatario EN riceve la copia in inglese). **Gated** su `NDA_COPY_TO_COMPANY=1`: col mittente di prova `onboarding@resend.dev` Resend consegna solo all'account owner, quindi la copia a terzi è recapitabile solo con un dominio verificato (`RESEND_FROM`) — l'owner accende il flag quando ha il dominio. **FIRE-AND-FORGET (fix bug latenza, review Fable+GPT su #29):** attendere la copia in serie dopo l'invio owner portava il budget server a ~30s contro il timeout client di 15s → se owner ok ma copia lenta, il client cade su `FAIL("network")` mentre la firma è già registrata (anti-replay mantenuto) → retry `409 already_signed` e utente escluso senza password. Ora la copia parte in **background** (IIFE `void`), fuori dal percorso critico: la latenza resta quella del solo invio owner. Best-effort: errori solo loggati **senza PII** (solo `signatureId`, mai il messaggio grezzo del provider). Semantica onesta: la risposta porta `companyCopyRequested` (invio AVVIATO, non conferma di consegna) e lo sblocco mostra «Stiamo inviando una copia … (controlla anche lo spam): …». Aggiunto il tag wordmark **[B2B DEMO]** (dai commenti #26). Test hard (mock di `resend`, nessuna rete): gating off/on, env-gating, invio separato + `replyTo` owner, localizzazione IT/EN, fire-and-forget su copia fallita/che-lancia (nessun unhandled rejection), errore owner→nessuna copia, non-persistenza di `companyCopyRequested`, propagazione client+server. 253 test verdi. **By-design ribaditi in-thread su #29 (nessuna patch):** i18n a 4 lingue (eccezione owner modulo business IT/EN), idempotenza su risposta persa (PR3), verifica email anti-abuso invio a terzi (PR3 — mitigato da gating+rate-limit+anti-replay). **PR3 punto 1 — store durevole (Postgres) + idempotenza (#30, PR successiva a #29):** l'anti-replay era in-memory (effimero: si azzera al redeploy Railway) e il 409 «una demo per email» **non distingueva** un retry dopo risposta persa da un nuovo tentativo → un utente che perdeva la risposta HTTP restava escluso senza password. Ora: (1) **`idempotencyKey` generata e persistita dal client** per email PRIMA dell'invio (`idempotencyKeyFor` in `demoSession.ts`, `nanoid`, `localStorage`); (2) `SignatureStore` ridisegnato: `reserve(entry)` è un check-and-set atomico che classifica `reserved`/`replay`(stessa chiave → recupero sessione con stessi signatureId+password+startedAt+esito email)/`duplicate_email`(email già firmata con chiave DIVERSA → 409); `finalize` aggiorna l'esito email per un replay fedele; `release` invariato. Lo store memorizza ora anche `password`+`ndaVersion`+esito email (la password è un token di sessione a bassa sensibilità, necessaria al replay). (3) **Impl. Postgres** (`server/nda/pgStore.ts`, dipendenza `pg`) attiva con `DATABASE_URL`, altrimenti **fallback in-memory** (come l'email senza `RESEND_API_KEY`); con `DATABASE_URL` PRESENTE un init Postgres fallito NON degrada in silenzio — l'errore si propaga e lo startup fallisce (`process.exit(1)`, vedi fix (b) sotto), così una config rotta è evidente invece di perdere la durabilità di nascosto (il degrado all'in-memory resta solo quando `DATABASE_URL` è ASSENTE). Atomicità dai vincoli PRIMARY KEY(`idempotency_key`)/UNIQUE(`business_email`) con `INSERT ... ON CONFLICT DO NOTHING`; schema creato all'avvio (`ensureSchema`, `CREATE TABLE IF NOT EXISTS`). Test hard: store in-memory (reserve/replay/duplicate/finalize/release), **Postgres via `pg-mem`** (SQL reale, nessun DB esterno), processNdaSign (idempotenza: retry stessa chiave → 200 stessa sessione, nessuna seconda email; anti-replay: chiave diversa stessa email → 409; concorrenza con chiavi diverse → 200+409), client (`submitNda` invia la chiave; `idempotencyKeyFor` persiste/rigenera). 264 test verdi. `pg` è server-only (non nel bundle client). **Azione owner per attivare la durabilità:** provisionare un Postgres su Railway e impostare `DATABASE_URL`; senza, il comportamento resta quello in-memory di #28/#29. Restano al PR3: sessione/token server-authoritative + `expiresAt`, verifica email, outbox/retry durevole per la copia all'azienda, font Unicode PDF, **retention righe Postgres**: implementato un cleanup **opportunistico** a ogni `reserve` (DELETE delle righe più vecchie di 30 giorni, best-effort in try/catch così un suo fallimento non blocca la firma; indice su `created_at` per non fare full-scan) → la `password` non resta at-rest a tempo indefinito e l'anti-replay diventa una finestra scorrevole. **Cleanup schedulato (chiude la lacuna del solo-opportunistico):** la retention non è più applicata solo al successivo `reserve` (che con traffico nullo lascerebbe la password at-rest oltre la finestra): un timer `setInterval` best-effort (`unref`, ogni 6h) in `server/index.ts` chiama `pruneExpired(pool)` anche senza firme, con un prime al boot; il prune opportunistico in `reserve` resta come finestra scorrevole fra un tick e l'altro. `pruneExpired` è esportata, best-effort (logga e non rilancia: né la firma né il timer cadono per un errore del DELETE), testata con pg-mem (pota le scadute senza reserve; non rilancia su pool rotto). L'eliminazione TOTALE del plaintext resta al PR3 punto 2 (sessione server-authoritative + `expiresAt` che riconvalida il `signatureId` lato server). **Fix bug TTL (review Fable su #31):** il TTL della chiave client (`IDEM_TTL_MS`) è stato portato a **45 giorni**, STRETTAMENTE MAGGIORE della retention server (`ROW_RETENTION_MS` = 30gg) con **margine** per clock skew/ritardi: prima era 1h (retry dopo >1h → chiave rigenerata → `duplicate_email`/409/lockout); poi allinearlo esattamente a 30gg lasciava un caso-limite (la retention server è una soglia opportunistica = minimo, non massimo), quindi ora client 45 > server 30. Un **test d'invariante** (`IDEM_TTL_MS > ROW_RETENTION_MS`, costanti esportate) blocca la regressione. **Limite residuo (annotato):** l'invariante protegge il margine ma non l'estremo opposto — se il prune fallisse ripetutamente e una riga sopravvivesse oltre i 45gg del TTL client, la chiave verrebbe rigenerata a riga ancora presente → `duplicate_email` 409 (il lockout che il fix elimina). Il cleanup schedulato ogni 6h (best-effort, oltre a quello opportunistico) rende il caso remoto; l'eliminazione definitiva è la sessione server-authoritative del PR3 punto 2. **Riconciliazione con la Nota PR2:** quella nota prometteva che con la sessione server-authoritative la `password` NON sarebbe stata persistita in chiaro; il replay QUI richiede la password memorizzata (per restituire la STESSA sessione dopo una risposta persa), quindi è un compromesso INTERIM voluto, mitigato da retention 30gg — il plaintext at-rest sarà eliminato dal PR3 punto 2 (riconvalida server del `signatureId`). **Init Postgres con retry:** `ensureSchema` è ritentata con backoff (max 5, 1→10s) prima di far fallire lo startup, così un'indisponibilità TRANSITORIA del DB non crasha al primo colpo. **Fix robustezza (review Fable+GPT su #31):** (a) `reserve` classifica `replay` SOLO se combaciano `idempotencyKey` E email normalizzata → una collisione dell'indice client (o il riuso della chiave con un'altra email) non restituisce mai la sessione di un altro (test in-memory + pg-mem); risolve anche la coerenza di normalizzazione client↔server (`normalizeEmail` su entrambi). (b) Selezione store: con `DATABASE_URL` PRESENTE un init Postgres fallito NON degrada in silenzio all'in-memory (nasconderebbe una config rotta) → si propaga e lo startup fallisce rumorosamente (`process.exit(1)`); il fallback in-memory resta solo quando `DATABASE_URL` è ASSENTE (dev). (c) La Privacy Policy dice «identificatore derivato dall'email» (non «hash», che sovrastimava la protezione: FNV-1a serve solo a non tenere l'email in chiaro at-rest, non è anonimizzazione forte). L'`idempotencyKey` non finisce nei log (audit logga solo `signatureId` + email mascherata).
